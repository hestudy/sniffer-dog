package com.snifferdog.ui.theme

import androidx.compose.ui.graphics.Color

/** Design tokens from home_entry / Fluid Media PRD. */
object SnifferColors {
    /** Design primary (#0DDBF2); close to PRD cyan glow. */
    val Primary = Color(0xFF0DDBF2)
    val Background = Color(0xFF050914)
    val Surface = Color(0x0AFFFFFF) // rgba(255,255,255,0.04)
    val Border = Color(0x14FFFFFF) // rgba(255,255,255,0.08)
    val Text = Color(0xFFF8FAFC)
    val Muted = Color(0xFF64748B)
    val Placeholder = Color(0xFF64748B)
    val Accent = Color(0xFFB400FF)
    val Success = Color(0xFF10B981)
}
